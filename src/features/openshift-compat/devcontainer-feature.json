{
  "id": "openshift-compat",
  "version": "2.1.1",
  "name": "OpenShift Compatibility",
  "description": "Make devcontainer images compatible with OpenShift restricted SCC: SSH server, random UID support, fake sudo, group-writable home, entrypoint with Paseo startup.",
  "keywords": [
    "openshift",
    "ssh",
    "security",
    "scc"
  ],
  "options": {
    "sshPort": {
      "type": "string",
      "default": "2222",
      "description": "Port for the SSH server to listen on"
    }
  },
  "entrypoint": "/usr/local/bin/entrypoint.sh",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
