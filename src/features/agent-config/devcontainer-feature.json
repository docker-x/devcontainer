{
  "id": "agent-config",
  "version": "1.0.0",
  "name": "Shared Agent Config",
  "description": "Creates a shared directory for AI agent configuration and exposes it as AGENT_CONFIG_DIR so multiple agent features can store their configs in one place.",
  "documentationURL": "https://github.com/docker-x/devcontainers#agent-config",
  "keywords": [
    "ai",
    "agents",
    "config",
    "shared"
  ],
  "containerEnv": {
    "AGENT_CONFIG_DIR": "/usr/local/share/agent-config"
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "options": {}
}
