{
  "id": "herdr",
  "version": "2.1.0",
  "name": "Herdr",
  "description": "Install Herdr - AI agent herd orchestration for coordinating multiple coding agents.",
  "keywords": [
    "ai",
    "herdr",
    "agents",
    "orchestration",
    "herd"
  ],
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of Herdr to install"
    },
    "shareConfig": {
      "type": "boolean",
      "default": false,
      "description": "When true, symlink the agent's config dir to the shared AGENT_CONFIG_DIR so config persists across container rebuilds and is shared with other agents"
    }
  },
  "dependsOn": {
    "ghcr.io/docker-x/devcontainers/agent-config": {}
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "ghcr.io/devcontainers/features/node",
    "ghcr.io/docker-x/devcontainers/agent-config"
  ]
}
